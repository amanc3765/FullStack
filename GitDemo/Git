Git Client
_______________________________

Local setup:
	git --version

	git config --global user.name "Aman Choudhary"
	git config --global user.email "amanc@iisc.ac.in"
	git config --list 

	git init

Add your files, and commit them.

Connect to server:
	git remote add origin 'git@github.com:amanc3765/FullStack.git'
	git remote -v
	git push -u origin master

SSH:
	ssh-keygen -t rsa -C amanc@iisc.ac.in
	Copy ~/.ssh/id_rsa.pub and add it to Github
	ssh -T git@github.com -> To check if keys were added properly

Change branch:
	git checkout <existing_branch>
